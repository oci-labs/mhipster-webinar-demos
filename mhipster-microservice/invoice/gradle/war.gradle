apply plugin: "war"

bootWar {
    mainClassName = "com.mhipster.demo.invoice.InvoiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
