# ===================================================================
# Micronaut Distributed Configuration bootstrap
# ===================================================================
consul:
  client:
    registration:
      enabled: false
    config:
      enabled: false
